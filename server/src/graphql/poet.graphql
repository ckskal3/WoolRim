type Poet {
  id: ID!
  name: String!
}

input CreatePoetInput {
  name: String!
}

input UpdatePoetInput {
  id:ID!
  name: String
}

type PoetResult {
  isSuccess: Boolean!
  msg: String
}

type Query {
  getPoet(id: ID!): Poet
  getAllPoet: [Poet]
}

type Mutation {
  createPoet(input_list: [CreatePoetInput]!): PoetResult!
  updatePoet(input: [UpdatePoetInput]): PoetResult!
  deletePoet(id_list: [ID]!): PoetResult!
}