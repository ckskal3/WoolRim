type Notice {
  id: ID!
  content: String!
  date: String!
  user_id: ID!
}
input CreateNoticeInput {
  content: String!
  user_id: ID!
}

input UpdateNoticeInput {
  content: String
}

type Query {
  getNotice(id: ID!): Notice
  getAllNotice: [Notice]
}

type Mutation {
  createNotice(input: CreateNoticeInput!): String!
  updateNotice(id: ID!, input: UpdateNoticeInput): String!
  deleteNotice(id: ID!): String!
}